# Notion operating context used

Global rules page read:
https://www.notion.so/3435a17a475f818bae05c4dca1bb6aba

Fetched page title: Second Brain Operating Rules

Rules applied to this study:

- Resolve project identity before work and declare repository/environment/source
  of truth.
- Keep project scopes separated.
- Record repo path, server path, public URL, API entrypoint, version and
  validation.
- Keep evidence in repo docs and do not store raw secrets in docs or logs.
- Make the current source of truth explicit and expose drift instead of
  silently mixing snapshots.

Notion search for the exact project string "green.cutdee.com" did not return a
dedicated current project page. Project identity was therefore resolved from
the user-supplied production URL, the active repository
/Users/sj88/Documents/codex/V3_cursor_API, the deployed Nginx configuration,
and the live API service name. This is not a guess of another project.

No Notion page was mutated in this read-only study.
